# the paper itself is not published in this repo (under review)
paper/

# raw data (auto-downloaded from the UCI repository on first run)
code/data/*
!code/data/.gitkeep

# python
__pycache__/
*.pyc
.ipynb_checkpoints/

# LaTeX build artifacts
*.aux
*.log
*.out
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.nav
*.snm
*.vrb
*.toc

# --- jupytext notebook pairs (auto-generated by the Jupyter config's
#     default_jupytext_formats = "ipynb,py:percent"; the .ipynb is the source
#     of truth). Listed by name so the real modules stay tracked. ---
baseline_and_federated_methods.py
baseline_full_scale_accuracy.py
consent_churn_study.py
mlp_amplification.py

# LaTeX build artifacts inside paper/ (paper/ itself is already ignored)
paper/tables/
